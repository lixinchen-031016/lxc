#include<iostream>
using namespace std;
/*void sort(int a[], int n)
{
	int j, temp, i;
	for (i = 0; i < n - 1; i++)
	{
		for (j = i + 1; j < n; j++)
		{
			if (a[i] > a[j])
			{
				temp = a[i];
				a[i] = a[j];
				a[j] = temp;
			}
		}
	}
}
int main() {
	int n, i, a[1000];
	cout << "请输入数组大小(元素个数):" << endl;
	cin>>n;
	cout << "请依次输入元素:" << endl;
	for (i = 0; i < n; i++)
	{
		cin>>a[i];
	}
	cout << "排序前:" << endl;
	for (i = 0; i < n; i++)
	{
		printf("%d", a[i]);
	}
	cout << "\n" << endl;
	cout << "排序后:" << endl;
	sort(a, n);
	for (i = 0; i < n; i++)
	{
		printf("%d", a[i]);
	}
	cout << "\n" << endl;
	return 0;
}*/
